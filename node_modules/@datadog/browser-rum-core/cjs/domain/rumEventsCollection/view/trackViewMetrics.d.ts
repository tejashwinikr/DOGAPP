import { Duration, Configuration } from '@datadog/browser-core';
import { ViewLoadingType } from '../../../rawRumEvent.types';
import { LifeCycle } from '../../lifeCycle';
import { EventCounts } from '../../trackEventCounts';
import { DOMMutationObservable } from '../../../browser/domMutationObservable';
export interface ViewMetrics {
    eventCounts: EventCounts;
    loadingTime?: Duration;
    cumulativeLayoutShift?: number;
}
export declare function trackViewMetrics(lifeCycle: LifeCycle, domMutationObservable: DOMMutationObservable, scheduleViewUpdate: () => void, loadingType: ViewLoadingType, configuration: Configuration): {
    stop: () => void;
    setLoadEvent: (loadEvent: Duration) => void;
    viewMetrics: ViewMetrics;
};
