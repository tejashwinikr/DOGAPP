import { Context, Duration, ClocksState, Configuration } from '@datadog/browser-core';
import { ActionType } from '../../../rawRumEvent.types';
import { LifeCycle } from '../../lifeCycle';
import { DOMMutationObservable } from '../../../browser/domMutationObservable';
declare type AutoActionType = ActionType.CLICK;
export interface ActionCounts {
    errorCount: number;
    longTaskCount: number;
    resourceCount: number;
}
export interface CustomAction {
    type: ActionType.CUSTOM;
    name: string;
    startClocks: ClocksState;
    context?: Context;
}
export interface AutoAction {
    type: AutoActionType;
    id: string;
    name: string;
    startClocks: ClocksState;
    duration: Duration;
    counts: ActionCounts;
    event: Event;
}
export interface AutoActionCreatedEvent {
    id: string;
    startClocks: ClocksState;
}
export declare function trackActions(lifeCycle: LifeCycle, domMutationObservable: DOMMutationObservable, configuration: Configuration): {
    stop(): void;
};
export {};
