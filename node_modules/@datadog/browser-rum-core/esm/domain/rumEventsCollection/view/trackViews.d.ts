import { Duration, ClocksState, TimeStamp, Configuration } from '@datadog/browser-core';
import { DOMMutationObservable } from '../../../browser/domMutationObservable';
import { ViewLoadingType, ViewCustomTimings } from '../../../rawRumEvent.types';
import { LifeCycle } from '../../lifeCycle';
import { EventCounts } from '../../trackEventCounts';
import { Timings } from './trackInitialViewTimings';
export interface ViewEvent {
    id: string;
    name?: string;
    location: Readonly<Location>;
    referrer: string;
    timings: Timings;
    customTimings: ViewCustomTimings;
    eventCounts: EventCounts;
    documentVersion: number;
    startClocks: ClocksState;
    duration: Duration;
    isActive: boolean;
    loadingTime?: Duration;
    loadingType: ViewLoadingType;
    cumulativeLayoutShift?: number;
}
export interface ViewCreatedEvent {
    id: string;
    name?: string;
    location: Location;
    referrer: string;
    startClocks: ClocksState;
}
export interface ViewEndedEvent {
    endClocks: ClocksState;
}
export declare const THROTTLE_VIEW_UPDATE_PERIOD = 3000;
export declare const SESSION_KEEP_ALIVE_INTERVAL: number;
export declare function trackViews(location: Location, lifeCycle: LifeCycle, domMutationObservable: DOMMutationObservable, areViewsTrackedAutomatically: boolean, configuration: Configuration, initialViewName?: string): {
    addTiming: (name: string, time?: TimeStamp) => void;
    startView: (name?: string | undefined, startClocks?: ClocksState | undefined) => void;
    stop: () => void;
};
