import { Configuration, RelativeTime, Duration } from '@datadog/browser-core';
import { InForegroundPeriod } from '../rawRumEvent.types';
export declare const MAX_NUMBER_OF_FOCUSED_TIME = 500;
export interface ForegroundContexts {
    getInForeground: (startTime: RelativeTime) => boolean | undefined;
    getInForegroundPeriods: (startTime: RelativeTime, duration: Duration) => InForegroundPeriod[] | undefined;
    stop: () => void;
}
export interface ForegroundPeriod {
    start: RelativeTime;
    end?: RelativeTime;
}
export declare function startForegroundContexts(configuration: Configuration): ForegroundContexts;
export declare function addNewForegroundPeriod(): void;
export declare function closeForegroundPeriod(): void;
