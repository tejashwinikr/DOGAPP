export interface Subscription {
    unsubscribe: () => void;
}
export declare class Observable<T> {
    private observers;
    subscribe(f: (data: T) => void): Subscription;
    notify(data: T): void;
}
