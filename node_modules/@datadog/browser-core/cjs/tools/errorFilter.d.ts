import { Configuration } from '../domain/configuration';
import { RawError } from './error';
export declare type ErrorFilter = ReturnType<typeof createErrorFilter>;
export declare function createErrorFilter(configuration: Configuration, onLimitReached: (limitError: RawError) => void): {
    isLimitReached(): boolean;
};
