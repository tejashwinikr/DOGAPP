import { BuildEnv } from '../../boot/init';
import { InitConfiguration } from './configuration';
export interface TransportConfiguration {
    logsEndpoint: string;
    rumEndpoint: string;
    sessionReplayEndpoint: string;
    internalMonitoringEndpoint?: string;
    isIntakeUrl: (url: string) => boolean;
    replica?: ReplicaConfiguration;
}
export interface ReplicaConfiguration {
    applicationId?: string;
    logsEndpoint: string;
    rumEndpoint: string;
    internalMonitoringEndpoint: string;
}
export declare function computeTransportConfiguration(initConfiguration: InitConfiguration, buildEnv: BuildEnv, isIntakeV2Enabled?: boolean): TransportConfiguration;
