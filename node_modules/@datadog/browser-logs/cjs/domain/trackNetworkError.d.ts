import { Configuration, Observable, RawError } from '@datadog/browser-core';
export declare function trackNetworkError(configuration: Configuration, errorObservable: Observable<RawError>): {
    stop: () => void;
};
