"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toFormEntries = exports.send = exports.SEND_BEACON_BYTE_LENGTH_LIMIT = void 0;
var browser_core_1 = require("@datadog/browser-core");
exports.SEND_BEACON_BYTE_LENGTH_LIMIT = 60000;
function send(endpointUrl, data, meta, rawSegmentSize) {
    var formData = new FormData();
    formData.set('segment', new Blob([data], {
        type: 'application/octet-stream',
    }), meta.session.id + "-" + meta.start);
    toFormEntries(meta, function (key, value) { return formData.set(key, value); });
    formData.set('raw_segment_size', rawSegmentSize.toString());
    var request = new browser_core_1.HttpRequest(endpointUrl, exports.SEND_BEACON_BYTE_LENGTH_LIMIT);
    request.send(formData, data.byteLength);
}
exports.send = send;
function toFormEntries(input, onEntry, prefix) {
    if (prefix === void 0) { prefix = ''; }
    browser_core_1.objectEntries(input).forEach(function (_a) {
        var key = _a[0], value = _a[1];
        if (typeof value === 'object' && value !== null) {
            toFormEntries(value, onEntry, "" + prefix + key + ".");
        }
        else {
            onEntry("" + prefix + key, String(value));
        }
    });
}
exports.toFormEntries = toFormEntries;
//# sourceMappingURL=send.js.map